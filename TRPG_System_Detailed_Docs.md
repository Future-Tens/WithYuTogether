# 🔥 永恆深淵的餘燼 - 系統開發說明書 (V1.1.0 數據驅動版)

## 1. 系統架構：數據驅動 (Data-Driven)
本系統核心邏輯與數值完全分離。透過修改 JSON 檔案即可調整平衡性，無需變動 Python 程式碼。

### 核心設定檔
- `trpg_jobs.json`: 定義職業初始屬性、升級成長點數與戰鬥特性。
- `trpg_content.json`: 定義怪物原型、隨機事件與道具資料庫。
- `trpg_users.json`: 存放玩家持久化存檔。

---

## 2. 職業參數化系統
所有職業特性皆透過 `trpg_jobs.json` 進行參數化配置：

- **`init_stats`**: 基礎四大屬性 (STR, DEX, INT, PER)。
- **`hp_mod`**: 生命上限修正值 (以 50 為基準)。
- **`sp_per_level`**: 每次升級獲得的自由分配點數。
- **`combat_config`**:
    - `crit_chance`: 暴擊率 (0.0 ~ 1.0)。
    - `crit_mult`: 暴擊傷害倍率。
    - `hp_cost_per_atk`: 每回合攻擊消耗的生命值。
    - `flat_bonus_atk`: 每回合額外增加的固定傷害。

---

## 3. 怪物生成與數值烘焙 (Monster Baking)
為了確保 UI 顯示一致性與效能，怪物數值在 `generate_monster` 時即完成計算：

### 縮放公式
1. **關卡縮放 (Stage Scaling)**: `1 + (stage * 0.02)` (每層強度提升 2%)。
2. **房間倍率 (Type Scaling)**:
    - **精英**: HP x1.5, ATK x1.2, EXP x1.5, 掉落率 x2。
    - **魔王**: HP x3.0, ATK x1.5, EXP x3.0, 掉落率 100%。

**最終數值 = 基礎值 × 關卡縮放 × 房間倍率** (結果皆取整數)。

---

## 4. 戰鬥引擎邏輯 (TRPGEngine)
引擎僅負責純粹的數值交換，不再包含硬編碼的職業名稱判斷：

- **玩家傷害**: `((STR // 1.5) * 暴擊倍率) + 1d6 + 固定加成 - 怪物防禦`。
- **怪物傷害**: `怪物攻擊力 - (玩家敏捷 // 玩家減傷除數)`。
- **生存判定**: 當 `HP <= 0` 時觸發 `end_adventure`。

---

## 5. 探索流程與狀態旗標
- **`pending_routes`**: 進入新層級前預先生成兩條路線並存入 JSON，防止玩家透過重複輸入指令刷掉難關。
- **`event_ready`**: 布林值。當關卡到達 5 的倍數時設為 `True`，確保休息點在正確時機出現且不會卡死。